import { styled } from 'styled-components'

export const Panel = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1;
  padding: 0.5rem 0;
  user-select: text;
  overflow-x: hidden;
  overflow-y: auto;
`
